# AGENTS.md

## Overview

This is a **GitHub profile README repository** (`Fuadrahma/Fuadrahma`). It contains a single `README.md` that is rendered on the owner's GitHub profile page. There is no application code, no dependencies, no build system, and no services.

## Cursor Cloud specific instructions

- **No dependencies to install.** The repository contains only `README.md` (Markdown).
- **No lint, test, or build commands.** Standard code tooling does not apply.
- **No services to run.** There is no backend, frontend, or database.
- **Editing workflow:** Edit `README.md`, commit, and push. GitHub renders it automatically on the profile page.
- **Preview:** You can preview the rendered Markdown locally using `grip README.md` (Python package) or any Markdown previewer, but this is optional.
